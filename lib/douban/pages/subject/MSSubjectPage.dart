import 'package:flutter/material.dart';

class MSSubjectPage extends StatelessWidget {
  const MSSubjectPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("书影音"),
      ),
      body: Text("书影音"),
    );
  }
}
